# BGP: Better Github Project

A new github UI that aims to replicate the original look-and-feel while being more performant and usable.

## Development

Dev Server:

```bash
bun run dev
```

Build (ensure typescript works):

```bash
bun run build
```

Format (prettier):

```bash
bun run format
```